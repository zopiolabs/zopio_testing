# Auth Log Configuration

# Logging target (console, file, betterstack)
AUTH_LOG_TARGET=console

# BetterStack configuration (required if AUTH_LOG_TARGET=betterstack)
BETTERSTACK_SOURCE_TOKEN=your_source_token_here

# Optional BetterStack endpoint override (defaults to https://in.logs.betterstack.com)
# BETTERSTACK_ENDPOINT=https://custom-endpoint.example.com
